version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.14"
  jobs:
    pre_build:
      - pip install -e .
      - sphinx-apidoc -o docs/source/ src/ctdam

sphinx:
  configuration: docs/conf.py

python:
  install:
    - requirements: docs/requirements.txt
